import grey from '@material-ui/core/colors/grey';
import styled from '@emotion/styled';

export const StyledAppContentWrap = styled('div')({
  display: 'flex',
  flexDirection: 'column',
  width: '100%',
  backgroundColor: grey[200],
});